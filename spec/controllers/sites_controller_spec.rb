require 'rails_helper'

describe SitesController, :type=> :controller do
  #before do
  #  @site = Site.new
  #end

  #describe "should update site" do
  #   it "update site params" do
  #      patch site_url(@site), params: { site: { suspended: @site.suspended } }
  #      assert_redirected_to site_url(@site)
  #   end
  #end
end
